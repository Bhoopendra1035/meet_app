{
  "name": "meet-app-deployment",
  "version": "1.0.0",
  "description": "Full stack LiveKit Meeting App",
  "scripts": {
    "install": "cd server && npm install && cd ../client && npm install --legacy-peer-deps",
    "build": "cd client && npm run build",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
